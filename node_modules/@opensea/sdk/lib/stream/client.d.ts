import { type BaseStreamMessage, type Callback, type ClientConfig, type CollectionOfferEvent, EventType, type ItemCancelledEvent, type ItemListedEvent, type ItemMetadataUpdate, type ItemReceivedBidEvent, type ItemReceivedOfferEvent, type ItemSoldEvent, type ItemTransferredEvent, type OrderValidationEvent, type TraitOfferEvent } from "./types";
/**
 * Client for the OpenSea Stream API.
 *
 * @example
 * ```ts
 * import { OpenSeaStreamClient } from "@opensea/sdk/stream"
 *
 * const client = new OpenSeaStreamClient({ apiKey: "YOUR_API_KEY" })
 *
 * const unsubscribe = client.onItemListed("doodles-official", event => {
 *   console.log(event.payload.item.nft_id)
 * })
 * ```
 *
 * @category Main Classes
 */
export declare class OpenSeaStreamClient {
    private transport;
    private logLevel;
    private onEvent;
    constructor({ network, apiKey, token, apiUrl, connectOptions, logLevel, onError, onEvent, }: ClientConfig);
    private debug;
    private info;
    private error;
    connect: () => void;
    disconnect: (callback?: () => void) => void;
    private on;
    onItemMetadataUpdated: (collectionSlug: string, callback: Callback<ItemMetadataUpdate>) => () => void;
    onItemCancelled: (collectionSlug: string, callback: Callback<ItemCancelledEvent>) => () => void;
    onItemListed: (collectionSlug: string, callback: Callback<ItemListedEvent>) => () => void;
    onItemSold: (collectionSlug: string, callback: Callback<ItemSoldEvent>) => () => void;
    onItemTransferred: (collectionSlug: string, callback: Callback<ItemTransferredEvent>) => () => void;
    onItemReceivedOffer: (collectionSlug: string, callback: Callback<ItemReceivedOfferEvent>) => () => void;
    onItemReceivedBid: (collectionSlug: string, callback: Callback<ItemReceivedBidEvent>) => () => void;
    onCollectionOffer: (collectionSlug: string, callback: Callback<CollectionOfferEvent>) => () => void;
    onTraitOffer: (collectionSlug: string, callback: Callback<TraitOfferEvent>) => () => void;
    onOrderInvalidate: (collectionSlug: string, callback: Callback<OrderValidationEvent>) => () => void;
    onOrderRevalidate: (collectionSlug: string, callback: Callback<OrderValidationEvent>) => () => void;
    /**
     * Subscribe to several event types on one collection with a single callback,
     * filtered server-side to the requested types.
     *
     * The callback receives `BaseStreamMessage<unknown>`: one subscription can
     * deliver several event types, so there is no single payload type to hand
     * back. `event_type` identifies which arrived, and the payload needs
     * narrowing before use.
     *
     * ```ts
     * client.onEvents(slug, [EventType.ITEM_SOLD, EventType.ITEM_LISTED], event => {
     *   if (event.event_type === EventType.ITEM_SOLD) {
     *     const sale = event as ItemSoldEvent
     *     console.log(sale.payload.sale_price)
     *   }
     * })
     * ```
     *
     * Use the typed single-event methods, such as `onItemSold`, when you want the
     * payload typed for you.
     *
     * @param collectionSlug collection slug, or `"*"` for all collections
     * @param eventTypes event types to subscribe to
     * @param callback invoked for every subscribed event type
     * @returns a function that removes all of this call's handlers
     */
    onEvents: (collectionSlug: string, eventTypes: EventType[], callback: Callback<BaseStreamMessage<unknown>>) => () => void;
}
