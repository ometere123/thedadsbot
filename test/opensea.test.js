import test from 'node:test';import assert from 'node:assert/strict';import {classifyOpenSeaPlan,extractApiTransaction} from '../packages/core/src/opensea.js';import {SEADROP_V1,OPENSEA_FEE_RECIPIENT} from '../packages/core/src/chains.js';import {MINT_PUBLIC_SELECTOR} from '../packages/core/src/intent-firewall.js';import {wordAddress,wordUint} from '../packages/core/src/hex.js';
const nft='0x1111111111111111111111111111111111111111',w='0x2222222222222222222222222222222222222222';
test('extracts nested API transaction defensively',()=>{const tx=extractApiTransaction({actions:[{transaction:{to:nft,data:'0x12345678',value:'9'}}]});assert.equal(tx.to,nft);assert.equal(tx.value,'9');});
test('non-SeaDrop API target is classified opaque and unapproved',()=>{const p=classifyOpenSeaPlan({apiPayload:{transaction:{to:nft,data:'0x12345678',value:'0'}},chainId:1,nftContract:nft,recipient:w,quantity:1});assert.equal(p.verification,'opaque');assert.equal(p.mode,'CONFIRM');assert.equal(p.requiresTargetApproval,true);assert.deepEqual(p.allowedTargets,[]);});
test('canonical SeaDrop public API plan upgrades to deterministic',()=>{const data=MINT_PUBLIC_SELECTOR+wordAddress(nft)+wordAddress(OPENSEA_FEE_RECIPIENT)+wordAddress('0x0000000000000000000000000000000000000000')+wordUint(1);const p=classifyOpenSeaPlan({apiPayload:{transaction:{to:SEADROP_V1,data,value:'1'}},chainId:8453,nftContract:nft,recipient:w,quantity:1});assert.equal(p.verification,'deterministic');});
